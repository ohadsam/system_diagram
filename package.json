{
  "name": "system-design-diagram-builder",
  "version": "1.0.0",
  "private": true,
  "description": "Client-only system design diagram builder (drag & drop AWS/architecture components), hosted on GitHub Pages.",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "test:unit": "node --test tests/unit/*.test.mjs",
    "test:e2e": "playwright test --config=tests/e2e/playwright.config.js",
    "test": "npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
